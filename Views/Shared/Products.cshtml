<header>

@Html.Raw(ViewBag.UserBag)

</header>

<div class="row">
  <div class="col-4">
    <div class="list-group" id="list-tab" role="tablist">
      <a class="list-group-item list-group-item-action active" id="list-home-list" data-toggle="list" href="#list-home" role="tab" aria-controls="home">Home</a>
      <a class="list-group-item list-group-item-action" id="list-profile-list" data-toggle="list" href="#list-profile" role="tab" aria-controls="profile">Profile</a>
      <a class="list-group-item list-group-item-action" id="list-messages-list" data-toggle="list" href="#list-messages" role="tab" aria-controls="messages">Messages</a>
      <a class="list-group-item list-group-item-action" id="list-settings-list" data-toggle="list" href="#list-settings" role="tab" aria-controls="settings">Settings</a>
    </div>
  </div>
  <div class="col-8">
    <div class="tab-content" id="nav-tabContent">
      <div class="tab-pane fade show active" id="list-home" role="tabpanel" aria-labelledby="list-home-list">
		    <div class ="buildingDiv" style="border-style: groove">	
			    @Html.Raw(ViewBag.BuildingBag)
		    </div>
      
      </div>
      <div class="tab-pane fade" id="list-profile" role="tabpanel" aria-labelledby="list-profile-list">
        <div class ="batteryDiv" style="border-style: groove">
           @Html.Raw(ViewBag.BatteryBag)
        </div>

      </div>
      <div class="tab-pane fade" id="list-messages" role="tabpanel" aria-labelledby="list-messages-list">
        <div class="columnDiv" style="border-style: groove">
            <!-- @Html.Raw(ViewBag.ColumnBag) -->
            <div class="row">
              <div class="col-3">INFO</div>
              <div class="col-6"></div>
              <div class="col-3">
                <input id="check" type="radio"> Select
              </div> 
            </div>
        </div>
      </div>
      <div class="tab-pane fade" id="list-settings" role="tabpanel" aria-labelledby="list-settings-list">
        <div class="elevatorDiv" style="border-style: groove">
           @Html.Raw(ViewBag.ElevatorBag)
        </div>
      </div>
    </div>
  </div>
</div>
<div style="margin-top:20px">
	<input class="btn btn-primary" style="" type="submit" value="Ask Intervention" id= "interventionFormButton">
</div>


<script>
$('div.columnDiv').click(function() {
  $n = event.target.nodeName;
  if ($n != 'INPUT') {
    $b2 = !$('#check').prop("checked");
    $('#check').prop("checked", $b2);
    //if change to true
    //submit event
    //else
    //no submit event
  }
})

</script>